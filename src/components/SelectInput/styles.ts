import styled from 'styled-components'

export const Container = styled.div`

    > select {
        background-color: var(--tertiary);
        color: var(--white);
        padding: 8px 10px;
        border-radius: 5px;
    }
`
